FROM python:3.8.10
WORKDIR /WORK_DOCKER

COPY *.py .          
#захуярюю в попередню папку (ворк) і копіюю всі .РУ
#комент в докері тільки на початку рядку канає

CMD ["sh", "-c", "tail -f /dev/null"]


docker tag python_docker:v1 levitskya/work_docker_hub[:v1]